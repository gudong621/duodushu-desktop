# ==================== AI 服务配置 ====================

# Google Gemini API Key
# 获取方式: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# DeepSeek API Key（备用，当 Gemini 失败时自动切换）
# 获取方式: https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here

# ==================== 数据库配置 ====================

# SQLite 数据库文件路径（默认：data/app.db）
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# ECDICT 词典数据库路径（默认：data/ecdict.db）
ECDICT_DB_PATH=./data/ecdict.db

# ==================== 服务器配置 ====================

# FastAPI 服务器端口（默认：8000）
PORT=8000

# CORS 允许的源（前端地址，多个用逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ==================== 词典配置 ====================

# MDX 词典数据目录（默认：dictionary）
DICTIONARY_PATH=./dictionary

# ==================== 日志配置 ====================

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径（默认：logs/backend.log）
LOG_FILE=./logs/backend.log

# ==================== 其他配置 ====================

# 文件上传目录（默认：uploads）
UPLOAD_DIR=./uploads

# 书籍封面缓存目录（默认：uploads/covers）
COVER_CACHE_DIR=./uploads/covers

# 音频缓存目录（默认：uploads/audio）
AUDIO_CACHE_DIR=./uploads/audio
